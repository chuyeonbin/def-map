import React from 'react';
import styles from './buttons.module.css';

const Buttons = ({ defStations, onButtonClick }) => {
  return <ul className={styles.buttons}></ul>;
};

export default Buttons;
